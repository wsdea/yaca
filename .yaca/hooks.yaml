attempt_completion:
  # - cmd: pytest -q -m "fast"
  #   on_error: blocking
  - cmd: pytest tests/tools
    on_error: blocking

done_coding:
  - cmd: ruff format --config ruff.toml .
    on_error: ignore
